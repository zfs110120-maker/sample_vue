// 曲线查询

const state =  {
  // currentFlow: 0,  //当前流量
}

const mutations = {
  // SETCURRENTFLOW(state,value=0) {  //设置当前流量
  //   state.currentFlow = value;
  // },
}
export default {
  namespaced: false,
  state,
  mutations
}